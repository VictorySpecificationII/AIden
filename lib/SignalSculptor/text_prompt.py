def get_text_prompt():
    """
    Capture a text prompt from the user via the command line.

    Returns:
        str: The user-entered prompt.
    """
    user_prompt = input("Enter your prompt: ")
    return user_prompt

def process_text_prompt(prompt):
    """
    Process a user-entered prompt.

    Args:
        prompt (str): The prompt entered by the user.

    Returns:
        str: The processed prompt.
    """
    # Placeholder function for processing logic
    processed_prompt = "Processed: " + prompt
    return processed_prompt