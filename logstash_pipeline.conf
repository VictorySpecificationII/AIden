# pipeline.conf
input {
  beats {
    port => 5045
  }
}

filter {
  # Your filter configurations
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "aiden-logs-%{+YYYY.MM.dd}"
  }
}
