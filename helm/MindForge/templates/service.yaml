apiVersion: v1
kind: Service
metadata:
  name: {{ include "MindForge.fullname" . }}
  labels:
    {{- include "MindForge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "MindForge.selectorLabels" . | nindent 4 }}
