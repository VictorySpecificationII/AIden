apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CaringCustodian.fullname" . }}-test-connection"
  labels:
    {{- include "CaringCustodian.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CaringCustodian.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
