apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "SignalSculptor.fullname" . }}-test-connection"
  labels:
    {{- include "SignalSculptor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "SignalSculptor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
