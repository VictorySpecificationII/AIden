apiVersion: v1
kind: Service
metadata:
  name: {{ include "SignalSculptor.fullname" . }}
  labels:
    {{- include "SignalSculptor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "SignalSculptor.selectorLabels" . | nindent 4 }}
