apiVersion: v1
kind: Service
metadata:
  name: {{ include "CodeCraft.fullname" . }}
  labels:
    {{- include "CodeCraft.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "CodeCraft.selectorLabels" . | nindent 4 }}
