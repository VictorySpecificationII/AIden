apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CodeCraft.fullname" . }}-test-connection"
  labels:
    {{- include "CodeCraft.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CodeCraft.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
