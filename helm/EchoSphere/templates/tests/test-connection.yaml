apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "EchoSphere.fullname" . }}-test-connection"
  labels:
    {{- include "EchoSphere.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "EchoSphere.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
