apiVersion: v1
kind: Service
metadata:
  name: {{ include "EchoSphere.fullname" . }}
  labels:
    {{- include "EchoSphere.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "EchoSphere.selectorLabels" . | nindent 4 }}
