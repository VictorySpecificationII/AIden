apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CyberGuardian.fullname" . }}-test-connection"
  labels:
    {{- include "CyberGuardian.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CyberGuardian.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
