apiVersion: v1
kind: Service
metadata:
  name: {{ include "CyberGuardian.fullname" . }}
  labels:
    {{- include "CyberGuardian.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "CyberGuardian.selectorLabels" . | nindent 4 }}
